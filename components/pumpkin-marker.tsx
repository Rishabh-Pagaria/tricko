import { View, StyleSheet } from 'react-native';
import { MaterialCommunityIcons } from '@expo/vector-icons';

export function PumpkinMarker() {
  return (
    <MaterialCommunityIcons 
      name="pumpkin" 
      size={40} 
      color="#FF7A1C"
    />
  );
}

const styles = StyleSheet.create({});